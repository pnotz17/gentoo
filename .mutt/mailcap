text/html; "$BROWSER" %s; test=test -n "$DISPLAY"; needsterminal;
video/x-youtube; mpv --really-quiet '%s'; needsterminal
video/*; mpv --really-quiet '%s'; needsterminal
text/plain; /usr/bin/nvim -f %s; 
#text/plain; cat; copiousoutput
application/pdf; "$BROWSER" %s; 
image/*; feh %s 
